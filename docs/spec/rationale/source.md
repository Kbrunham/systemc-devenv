# Spec source map (Confluence)

> TEMPLATE NOTE: After **"Use this template"**, list HAS pages, import phases, and ignored
> pages here. Upstream `systemc-devenv` keeps this as an empty scaffold.

Maps Confluence pages to `docs/spec/import/` files and tracks import status.

## Product configuration

| Field | Value |
|-------|-------|
| Confluence site | `_YOUR_SITE_.atlassian.net` |
| Space | `_SPACE_` |
| HAS root page ID | `_ROOT_PAGE_ID_` |
| Root URL | `_fill in_` |

## Page map

| Page ID | Title | Import file | Status |
|---------|-------|-------------|--------|
| — | — | — | planned |

Status values: `planned` | `imported (vN)` | `ignored`.

## Import phases

| Phase | Snapshot ID | Scope | Notes |
|-------|-------------|-------|-------|
| — | — | — | — |

## Ignored pages

Document title patterns or page IDs that must **not** be imported (duplicate exports,
drafts, etc.).

| Page ID / pattern | Reason |
|-------------------|--------|
| — | — |

## Manual fixups

Note any intentional post-import adjustments (macro cleanup, diagram path edits) that
agents must preserve on re-import.
